// import React from 'react';

import useTitle from "../useHook/useTitle";

const AddToy = () => {
    useTitle('Add Toy');
    return (
        <div>
        
        </div>
    );
};

export default AddToy;